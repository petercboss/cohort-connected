import React from 'react';

//components
import {Col} from '../../components/Grid';

//style
import './RSideBar.css';

const RSideBar = props => (
        <Col size='md-3 lg-3' className='LSideBar'> I am the Riggidy Riggidy Right-side bar!</Col>
  );

export default RSideBar;